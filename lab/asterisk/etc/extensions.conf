[globals]

[internal]
; phones dial each other
exten => 6001,1,Dial(PJSIP/6001,20)
exten => 6002,1,Dial(PJSIP/6002,20)

; echo test (classic Asterisk demo)
exten => 600,1,Answer()
 same => n,Playback(demo-echotest)
 same => n,Echo()
 same => n,Hangup()

; headless verification target for SIPp: answer, greet, echo media, caller hangs up
exten => 9000,1,Answer()
 same => n,Playback(hello-world)
 same => n,Echo()
 same => n,Hangup()
